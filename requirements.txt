# Web app framework
streamlit

# For environment variable loading
python-dotenv

# Data processing
pandas

# Language model (Groq + LangChain)
langchain
langchain-groq

# Prompt templating
openai  # Needed by LangChain for PromptTemplate base class

# Optional but common for CSV handling (already used)
numpy

# For Excel or complex CSV formats (if needed in future)
openpyxl
